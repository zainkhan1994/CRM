const assert=require('node:assert/strict');const G=require('../v2/inbox-groups.js');
const index=G.contactIndex([{id:'alice',name:'Alice',phone:'(832) 555-1212',email:'alice@example.com'},{id:'other-alice',name:'Alice',phone:'+19185551212'},{id:'shared1',name:'One',phone:'+12125551212'},{id:'shared2',name:'Two',phone:'+12125551212'}]);
const m=(id,email,extra={})=>({id,email,sender:email,channel:'Apple Messages',date:'2026-09-15T12:00:00Z',...extra});
const records=[m('1','+18325551212'),m('2','8325551212',{direction:'outgoing'}),m('3','alice@example.com',{channel:'Gmail'}),m('4','+19185551212'),m('5','+18325551212',{participants:['+18325551212','+19185551212'],threadId:'group-one'}),m('6','+18325551212',{participants:['+18325551212','+19185551212'],threadId:'group-two'})];
const groups=G.groupRecords(records,index);assert.equal(groups.size,4);assert.equal(groups.get('contact:alice').records.length,3);assert.equal(groups.get('contact:alice').incoming,true);assert.equal(G.resolve(m('shared','+12125551212'),index).contact,null);assert.notEqual(G.resolve(records[0],index).key,G.resolve(records[3],index).key);assert.notEqual(G.resolve(records[4],index).key,G.resolve(records[5],index).key);
const duplicate=G.contactIndex([{id:'a',name:'Alice',phones:['8325551212']},{id:'b',name:'Alice',phones:['+18325551212'],emails:['alice@example.com']}]);assert.equal(duplicate.get('18325551212').id,duplicate.get('alice@example.com').id);
const many=G.groupRecords(Array.from({length:100000},(_,i)=>m(String(i),'+18325551212')),index);assert.equal(many.size,1);assert.equal([...many.values()][0].records.length,100000);console.log('PASS: aliases, sent/received, shared-number ambiguity, separate group chats, duplicate contacts, 100k records');
